import 'package:flutter/material.dart';

class UdusBorderRadius {
  static final BorderRadius small = BorderRadius.circular(10);
  static final BorderRadius medium = BorderRadius.circular(15);
  static final BorderRadius large = BorderRadius.circular(20);
}
